# 输入参数

> 该文档是一个指令模板, 用于借助 AI agent 扩充和完善数学文档.

1. **数学输入格式**

- 文档采用Typst, 先阅读 [Typst官方文档](https://typst.app/docs) 了解语法.
- 阅读本地模板文件 `template.typ` 了解常用的数学符号.
- 对于行内公式, 使用 `$...$` 包裹; 对于独立行公式, 使用 `$ ... $` 包裹.

- 独立行公式的最后不需要任何符号, 写在一行内的全部用行内公式, 独立行公式必须单开一行. 下面的例子是错误的:

```typst
$ a + b $.
$ a + b. $
$ a + b $;
xxx $ a + b $, xxx /* 没有单开一行 */
```
- 统一使用半角标点.
- 正文优先自然分页, 不要为了让定义、命题或例子整块排在同一页而大量添加 `breakable: false`.
- 图表样式: 表格沿用模板的浅蓝隔行底色、居中内容、加粗表头及表头下细线, 不加竖线或外框; 使用 `table.header` 标记表头. 数学示意图统一使用蓝灰配色, 交换图使用 `book-diagram`.
- 术语约定: proper 统一译作"紧合", properness 译作"紧合性".
- 术语约定: smash product 统一译作"砸积".
- 术语约定: straightening 译作"拉直", unstraightening 译作"反拉直"; 英文仅在首次出现时标注.
- 算筹的 E 统一用 Typst 的 `EE` 表示, 例如 `$EE_1$`, `$EE_oo$`.
- 术语约定: 稳定无穷范畴语境中的 `QCoh` 对象称为 quasi-coherent module (拟凝聚模); 普通概形或普通代数叠上心中的对象称为 quasi-coherent sheaf (普通拟凝聚层).
- 记号约定: $G$-等变拟凝聚模范畴统一写作 `QCoh^G (X)`, 群放在上标.
- 记号约定: 稳定无穷范畴语境中的拉回、前推、整体截面统一写作 `f^*`, `f_*`, `Gamma`, 导出含义已经内蕴, 不加额外的导出前缀. `Gamma` 表示整体截面模谱, 普通截面群用 `H^0` 表示.
- 尽可能多使用 typst 的符号系统, 例如用 `->` 代替 `arrow`.
- 强调文本使用 `*...*` 粗体.
- 商对象统一使用横排斜线 `slash`, 例如 `$A slash I$`; `/` 保留给普通分式.
- typst的 `_` 和 `^` 会自动识别连续段, 如果要加括号不能直接加在后面, 例如 `f_i(x)` 会把 `i(x)` 识别为下标, 正确写法是 `f_i (x)`.加空格
- 记号约定: 用 $C$ 表示范畴, 并用小写字母 $u, v, w, dots$ 表示一般范畴中的对象. 当明确指定对象为 scheme 时, 改用大写字母 $U, V, W, dots$; 模空间等 scheme 也使用大写字母, 如 $M$.
- 记号约定: 环、域、环谱与模使用大写字母, 例如 `A, R, K, M, N, P`; 这优先于一般范畴对象用小写的约定. 环或模中的元素、态射与次数指标仍用小写.

2. **补充内容**

- 你需要根据上下文补充一些你觉得可以添加的内容.
- 纠正语法错误, 例如拼写错误, 语法错误, 逻辑错误等.
- 对于一些没有证明的命题, 你可以选择给出证明, 也可以选择不证明, 简短的证明或者详细的证明都可以, 你可以根据上下文来判断.
- 多添加例子和图表, 以便更好地说明问题.
- 添加一些能够助理解的东西, 例如一些注释, 解释, 说明, 直观的解释, 直观的图表等.
- 如果可以, 可以绘制一些 `svg` 图片. 但你最好保证不要绘制错误.
- 适当地对于某些容易混淆的概念, 进行一些解释和说明, 以便更好地理解.
- 对一些有难度的过程, 进一步拆解.
- 合理使用 `#remark` 来添加一些注释, 解释, 说明, 直观的解释.

3. **其他**

- 可以把文风改的更口语化, 更可爱, 可以加一些幽默的元素, emoji, 喵以及类似语气词.
