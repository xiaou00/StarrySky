#import "../template.typ":*
#import "@preview/cetz:0.4.1"
#import "@preview/fletcher:0.5.8" :*

#show: doc => conf(
  easy: true,
  doc,
)

= 意象 / Topoi <chap-topoi>

== Grothendieck 的几何哲学 <sec-grothendieck-geometry>

#chapter-ref(<chap-animae>)里, 三角形引导我们关注对象之间的变换及其同伦. 本章则从局部信息的粘合出发, 继续扩大"空间"的含义.

*亚历山大·格罗滕迪克* (Alexander Grothendieck, 1928-2014) 是这一转变的核心人物. 他出生于柏林, 在法国学习和从事数学研究, 早年研究泛函分析, 随后转向代数几何. 在法国高等科学研究所 (IHES) 工作期间, 他与合作者系统地重建了代数几何的基础, 并于 1966 年获得菲尔兹奖. 参见 @IHESGro26.

在回忆录 #emph[收获与播种] (#emph[Récoltes et Semailles]) 中, Grothendieck 用"海水上涨"的比喻谈论数学工作: 周围的理论逐渐丰富, 原先难以接近的问题也随之进入可以理解的范围. 他看重能够让许多定理共同生长出来的基本观念. 参见 @Gro86[英译本第 92-93 页]. 本章的抽象化也可以这样理解: *找到一种语言, 让分散的几何现象能够一起被理解*.

这首先表现为对*关系与变化*的重视. 例如, 映射 $w->cal(T)$ 表示一族随参数变化的三角形. 更一般地, 在几何中把态射 $u->v$ 看作以 $v$ 为底的一族对象, 沿 $w->v$ 作拉回来改变参数, 就体现了 Grothendieck 的*相对观点*.

同样, 我们可以从限制与粘合的关系来认识*局部*.

#example(title: "从局部函数开始")[
  设 $X$ 是拓扑空间, $cal(F)(U)$ 是开集 $U subset.eq X$ 上连续实值函数的集合. 对 $V subset.eq U$, 限制给出映射
  $ cal(F)(U)->cal(F)(V), quad f |-> f|_V $
  若 ${U_i}_(i in I)$ 开覆盖 $U$, 且 $f_i in cal(F)(U_i)$ 在交集 $U_i inter U_j$ 上取值相同, 就存在唯一的 $f in cal(F)(U)$, 满足
  $ f|_(U_i)=f_i, quad i in I $
  *层*抽象的正是这种规律: 相容的局部信息可以唯一粘合.
]

开集与包含映射决定信息怎样限制, 开覆盖决定何时局部信息已经足够. 记开集范畴为 $Open(X)$, 层便是反变函子 $Open(X)^opp->Set$, 再加上对覆盖的粘合条件.

Grothendieck 将这里的开集与包含推广为一般的局部对象与态射, 并重新规定哪些态射族算作覆盖. 代数几何中的平展覆盖就是重要的例子, 它让上同调能够用于研究算术问题. 参见 @Gro86[英译本第 75-81 页].

把覆盖的规则公理化, 就进入了 *Grothendieck 拓扑*的语言. 配备这种拓扑的范畴称为*景* (site); 小景上的集合值层组成*Grothendieck 意象* (Grothendieck topos), 后文简称*意象*. 这里的转变是: *用全部层及其关系组成的范畴, 来表达一种广义空间*. 参见 @Gro86[英译本第 77-80 页].

$ "局部对象与覆盖规则" -> "景" (C,J) -> "意象" Sh(C,J) $

本章先讨论集合值层, 以后再将局部信息换成生象, 引入高阶同伦的粘合. 下面先用覆盖族与筛两种方式, 把覆盖的规则说清楚.

=== 用覆盖族与纤维积描述局部性

设 $C$ 是一个范畴. 对象 $u in C$ 上的一个*覆盖族* 是一族具有共同目标的态射

$
cal(V)={f_i:u_i->u}_(i in I)
$

它应当被想成普通拓扑中的开覆盖 ${U_i subset.eq U}_(i in I)$. 为了使这种直觉在任意范畴中成立, 必须把开覆盖最基本的三个性质抽象出来.

#definition(title:[Grothendieck 预拓扑])[
  假设 $C$ 具有下面用到的纤维积. 一个*Grothendieck 预拓扑* (Grothendieck pretopology) 为每个对象 $u in C$ 指定一类覆盖族 $"Cov"(u)$, 并满足:

  - *同构覆盖*: 若 $f:v->u$ 是同构, 则单元素族 ${f:v->u}$ 覆盖 $u$.

  - *基变换稳定性*: 若 ${f_i:u_i->u}_(i in I)$ 覆盖 $u$, 且 $g:v->u$ 是任意态射, 则纤维积 $u_i times_u v$ 存在, 并且

    $
    {u_i times_u v -> v}_(i in I)
    $

    覆盖 $v$.

  - *传递性*: 若 ${f_i:u_i->u}_(i in I)$ 覆盖 $u$, 并且对每个 $i$ 都有覆盖族 ${g_(i j):v_(i j)->u_i}_(j in J_i)$, 则复合族

    $
    {f_i compose g_(i j):v_(i j)->u}_(i in I,j in J_i)
    $

    覆盖 $u$.
]

这三条公理分别说明: 局部模型可以覆盖自己; 覆盖拉回后仍是覆盖; 局部的局部仍然是局部. 其中纤维积扮演普通拓扑中交集的角色. 若 $u_i->u$ 与 $u_j->u$ 属于同一个覆盖族, 那么

$
u_i times_u u_j
$

就是两块局部对象的重叠部分.

#definition(title:[覆盖族版本的景与层])[
  带有上述覆盖族结构的范畴称为一个*景* (site). 若集合值预层 $cal(F):C^opp->Set$ 对每个覆盖族 ${u_i->u}_(i in I)$ 都使下图成为等化子图,

  $
  cal(F)(u) -> product_(i in I) cal(F)(u_i)
  ⇉
  product_((i,j) in I times I) cal(F)(u_i times_u u_j)
  $

  就称 $cal(F)$ 是这个景上的层. 两条平行箭头分别把 $u_i$ 与 $u_j$ 上的截面限制到纤维积 $u_i times_u u_j$.
]

这正是开头局部函数粘合规律的等化子表述. 原先的开集交被纤维积替代, "局部截面在交集上相等"则变成"两个拉回到纤维积的截面相等". 因此纤维积版本很适合直接进行粘合计算.

=== 用筛描述覆盖及其进一步限制

一个覆盖族只列出第一层局部对象. 若 $f:v->u$ 是其中一块, 那么经由 $f$ 分解的态射 $w->v->u$ 就表示这块局部对象上的进一步限制. *筛*把一个覆盖族连同它的所有进一步限制一次性收集起来.

#definition(title:[筛])[
  设 $u in C$. $u$ 上的一个*筛* (sieve) 是可表预层 $h_u=Hom_C (-,u)$ 的一个子预层

  $
  cal(R) subset.eq h_u
  $

  具体地, 对每个 $v in C$, 集合 $cal(R)(v) subset.eq Hom_C (v,u)$ 由一批以 $u$ 为目标的态射组成, 并且对预复合封闭: 若 $f:v->u$ 属于 $cal(R)(v)$, 且 $g:w->v$, 则

  $
  f compose g:w->u
  $

  属于 $cal(R)(w)$.
]

若 $f:v->u$ 是态射, $cal(R)$ 是 $u$ 上的筛, 则它沿 $f$ 的拉回筛 $f^* cal(R)$ 定义为

$
(f^* cal(R)) (w):={g:w->v:f compose g in cal(R)(w)}
$

这个定义只使用态射复合, 不需要事先选取纤维积. 最大筛 $h_u$ 包含所有以 $u$ 为目标的态射, 对应"$u$ 被自身完全覆盖".

#definition(title:[筛版本的 Grothendieck 拓扑])[
  范畴 $C$ 上的一个*Grothendieck 拓扑* $J$ 为每个对象 $u in C$ 指定一族筛 $J(u)$, 其中的元素称为 $u$ 上的*覆盖筛*, 并满足:

  - *最大筛覆盖*: $h_u in J(u)$.

  - *基变换稳定性*: 若 $cal(R) in J(u)$ 且 $f:v->u$, 则 $f^* cal(R) in J(v)$.

  - *局部性*: 设 $cal(R) in J(u)$, 而 $cal(S)$ 是 $u$ 上的任意筛. 若对每个 $f:v->u$ 且 $f in cal(R)(v)$, 拉回筛 $f^* cal(S)$ 都属于 $J(v)$, 则 $cal(S) in J(u)$.

  二元组 $(C,J)$ 称为一个*景*.
]

第三条公理可以读成: 若 $cal(R)$ 已经覆盖 $u$, 而 $cal(S)$ 在 $cal(R)$ 的每一块上都局部覆盖, 那么 $cal(S)$ 本身覆盖 $u$. 它正是覆盖族传递性的无坐标版本.

筛还给出了层条件更内在的表达. 对覆盖筛 $cal(R) subset.eq h_u$, 包含映射诱导限制

$
Hom_(PSh(C)) (h_u,cal(F))
->
Hom_(PSh(C)) (cal(R),cal(F))
$

左侧由 Yoneda 引理等同于 $cal(F)(u)$. 因而 $cal(F)$ 是景 $(C,J)$ 上的层, 当且仅当对每个覆盖筛 $cal(R) in J(u)$, 上述映射都是双射. 直观地说, 一个定义在覆盖筛全部局部箭头上, 并与进一步限制相容的截面族, 能且只能以一种方式延拓到整个可表预层 $h_u$.

=== 覆盖族与覆盖筛的对应

给定一族态射 $cal(V)={f_i:u_i->u}_(i in I)$, 它生成的筛 $cal(R)_cal(V)$ 由所有能够经过某个 $f_i$ 分解的态射组成. 也就是说, $g:v->u$ 属于这个筛, 当且仅当存在 $i in I$ 与态射 $h:v->u_i$ 使得

$
g=f_i compose h
$

一个 Grothendieck 预拓扑由此生成一个 Grothendieck 拓扑: 宣布筛 $cal(R)$ 覆盖 $u$, 当且仅当它包含某个覆盖族生成的筛. 在所需纤维积存在时, 反过来也可以把一族态射称为覆盖族, 当且仅当它生成的筛是覆盖筛.

#remark(title:[为什么筛更内在])[
  同一个覆盖筛可能由不同的覆盖族生成, 不同的预拓扑也可能给出完全相同的覆盖筛与层. 覆盖族适合计算, 因为纤维积直接写出二重交; 筛则忘掉生成元的选择, 只保留"哪些态射在局部上已经足够". 因此 Grothendieck 拓扑在概念上通常用筛定义, 实际验证层条件时则常回到覆盖族.
]

=== 普通拓扑空间如何包含在其中

现在回到拓扑空间 $X$. 取范畴 $C=Open(X)$, 对象是开集, 态射是包含映射. 对 $U_i,V subset.eq U$, 范畴中的纤维积正是开集交:

$
U_i times_U V=U_i inter V
$

声明包含族 ${U_i->U}_(i in I)$ 是覆盖族, 当且仅当

$
U=union_(i in I) U_i
$

预拓扑的三条公理此时分别变成: $U$ 覆盖自身; 若 ${U_i}$ 覆盖 $U$, 则 ${U_i inter V}$ 覆盖 $V$; 若每个 $U_i$ 又被 ${V_(i j)}_j$ 覆盖, 那么所有 $V_(i j)$ 一起覆盖 $U$. 这些恰好是普通开覆盖的基本性质.

从筛的角度看, $U$ 上的筛就是一族对取更小开集封闭的开子集. 这样的筛是覆盖筛, 当且仅当它所包含的全部开集之并等于 $U$. 因此景 $Open(X)$ 上的层条件, 正好回到拓扑空间中相容局部截面唯一粘合的条件.

#remark(title:[推广的真正含义])[
  Grothendieck 拓扑不是在范畴的对象集合上再放一个点集拓扑. 它推广的是"什么叫局部覆盖": 开子集被一般态射替代, 交集被纤维积替代, 对更小开集封闭的开集族被筛替代. 层论依赖的限制与粘合机制因此可以离开具体的点集空间而继续成立.
]

=== Grothendieck 意象

#definition(title:[Grothendieck 意象])[
  若一个范畴 $cal(E)$ 等价于某个小景 $(C,J)$ 上的集合值层范畴,

  $
  cal(E) tilde.eq Sh(C,J)
  $

  就称 $cal(E)$ 是一个*Grothendieck 意象* (Grothendieck topos), 简称*意象*.
]

#remark(title:[一个意象可以有多种景表示])[
  可能存在两个底层范畴和覆盖结构都不同的景 $(C,J)$ 与 $(D,K)$, 但它们的层范畴彼此等价:

  $
  Sh(C,J) tilde.eq Sh(D,K)
  $

  此时两个景表示同一个意象. 因此景不是意象唯一确定的组成部分, 而是描述意象的一套局部坐标; 真正内在的对象是层范畴本身及其范畴结构.
]

景 $(C,J)$ 是意象的一种表示: 它给出可用于计算的局部对象, 覆盖与限制映射. 意象 $Sh(C,J)$ 则是所有满足粘合条件的层所组成的范畴. 可以把两者的关系概括为

$
"景" (C,J) -> "意象" Sh(C,J)
$

这类似于用不同的坐标图册描述同一个几何对象. 在这种观点下, 景提供一种表示, 意象则可以被看作不依赖这套表示的广义空间. 接下来, 我们用范畴本身的性质刻画它.

若还要记录函数及其代数运算, 可以在意象 $cal(E)$ 上配备一个环对象 $cal(O)$, 得到*赋环意象* $(cal(E),cal(O))$. 例如概形的 Zariski 意象连同结构层, 同时记录局部覆盖与正则函数. 一般的赋环意象并不自动来自概形, 后者还须满足局部的仿射条件.

== 1-Giraud 定理 <sec-giraud>

以下固定一个小景 $(C,J)$, 并记

$
cal(E):=Sh(C,J)
$

再记 $i:cal(E)->PSh(C)$ 为包含函子, $a:PSh(C)->cal(E)$ 为层化函子. 我们先借助景的表示证明 Grothendieck 意象具有一系列类似 $Set$ 的正合性质, 最后再说明这些性质反过来足以刻画 Grothendieck 意象.

#proposition(title:[完备, 余完备与左正合层化])[
  Grothendieck 意象 $cal(E)$ 有所有小极限与小余极限. 对任意小范畴 $K$ 和图式 $cal(D):K->cal(E)$, 有

  $
  i (lim_(cal(E)) cal(D))
  tilde.eq
  lim_(PSh(C)) (i compose cal(D))
  $

  $
  colim_(cal(E)) cal(D)
  tilde.eq
  a (colim_(PSh(C)) (i compose cal(D)))
  $

  此外, 层化函子 $a$ 保持有限极限, 即它是一个*左正合* (left exact) 局部化.
]

#proofsketch[
  预层范畴是函子范畴, 所以极限与余极限都能在每个 $u in C$ 上逐点计算. 层条件本身是一组极限条件: 相容局部截面组成等化子, 更内在地说, 对覆盖筛 $cal(R) subset.eq h_u$ 要求

  $
  Hom_(PSh(C)) (h_u,cal(F)) -> Hom_(PSh(C)) (cal(R),cal(F))
  $

  为双射. 极限与极限可以交换, 因而一族层在预层范畴中的极限仍是层, 这给出第一条公式.

  预层的逐点余极限未必满足粘合, 所以还要施加一次层化. 由 $a$ 对 $i$ 的左伴随泛性质, 层化后的预层余极限正好满足 $cal(E)$ 中余极限的泛性质, 得到第二条公式.

  最后, 标准的加号构造用覆盖筛上的相容族构造 $cal(F)^+$, 再迭代得到层化. 在验证有限个截面之间的等式时, 可以把有限多个覆盖共同细化到同一个覆盖上; 而集合中的滤过余极限与有限极限交换. 因而这一构造保持终对象与纤维积, 也就保持所有有限极限.
]

#proposition(title:[切片仍是 Grothendieck 意象])[
  对任意对象 $s in cal(E)$, 切片范畴

  $
  cal(E) slash s
  $

  仍是一个 Grothendieck 意象.
]

#proofsketch[
  由 $s$ 构造一个新的小景 $(C_s,J_s)$. 它的对象是二元组 $(u,sigma)$, 其中 $u in C$ 且 $sigma in s(u)$. 从 $(v,tau)$ 到 $(u,sigma)$ 的态射是满足

  $
  tau=s(f) (sigma)
  $

  的态射 $f:v->u$. $(u,sigma)$ 上的筛是覆盖筛, 当且仅当它投影到 $C$ 后是 $u$ 上的覆盖筛.

  一个层态射 $x->s$ 在每个 $(u,sigma)$ 上给出映射 $x(u)->s(u)$ 在 $sigma$ 上的纤维; 这些纤维随限制映射组成 $(C_s,J_s)$ 上的层. 反过来, 把所有 $sigma in s(u)$ 上的纤维作不交并, 就重新得到一个带有映射到 $s$ 的层. 两个构造互逆, 因而

  $
  cal(E) slash s
  tilde.eq
  Sh(C_s,J_s)
  $
]

#proposition(title:[Cartesian 闭性与局部 Cartesian 闭性])[
  对任意 $x,y in cal(E)$, 存在指数对象 $y^x$, 满足对每个 $t in cal(E)$ 都有自然同构

  $
  Hom_cal(E) (t,y^x)
  tilde.eq
  Hom_cal(E) (t times x,y)
  $

  更强地, 每个切片 $cal(E) slash s$ 都是 Cartesian 闭的, 所以 $cal(E)$ 是*局部 Cartesian 闭* (locally Cartesian closed) 的.

  对态射 $f:x->y$, 拉回函子 $f^*:cal(E) slash y->cal(E) slash x$ 同时拥有左伴随 $Sigma_f$ 与右伴随 $Pi_f$:

  $
  Sigma_f tack.l f^* tack.l Pi_f
  $
]

#proofsketch[
  指数对象可以先在景上写出. 对 $u in C$, 令

  $
  y^x (u)
  :=
  Hom_cal(E) (a(h_u) times x,y)
  $

  态射 $v->u$ 经 Yoneda 嵌入给出 $h_v->h_u$, 对它层化并与 $x$ 取积, 再作预复合, 就得到 $y^x (u)->y^x (v)$. 这里 $u |-> h_u$ 对 $u$ 是协变的, 最后的 $Hom(-,y)$ 才使限制方向反转. 一族这样的局部态射若在覆盖上相容, 便能逐个分量粘合成唯一的整体态射, 所以 $u |-> y^x (u)$ 是层. Yoneda 引理与层化的泛性质给出所需指数伴随同构.

  前一个命题说明每个 $cal(E) slash s$ 本身仍是层范畴, 对它重复同一构造便得到局部 Cartesian 闭性. 对 $f:x->y$, $Sigma_f$ 只是把 $z->x$ 与 $f$ 复合成 $z->y$; 它是 $f^*$ 的左伴随. 切片中的指数对象则给出 $f^*$ 的右伴随 $Pi_f$, 即依赖积.
]

#remark(title:[与依赖类型的联系])[
  在切片 $cal(E) slash y$ 中, 对象可以看成以 $y$ 为参数的一族对象. 此时 $Sigma_f$ 表示依赖和, $Pi_f$ 表示依赖积, 而拉回 $f^*$ 表示代入或换基. 因而局部 Cartesian 闭性正是意象能够解释依赖类型论的范畴论原因之一.
]

#proposition(title:[像分解, 有效满射与有效等价关系])[
  任意态射 $f:x->y$ 都有像分解

  $
  x ->^e Im(f) ->^m y
  $

  其中 $e$ 是满射 (epimorphism), $m$ 是单射 (monomorphism). 这里使用范畴论意义的满射与单射. 层态射 $f:x->y$ 是满射, 当且仅当 $y$ 的每个截面都能局部提升到 $x$; 单射则等价于每个 $x(u)->y(u)$ 都是单射. 参见 @Stacks26[Tag 00WN].

  每个满射 $f:x->y$ 都是*有效满射*: 若

  $
  pr_1,pr_2:x times_y x -> x
  $

  是它的核偶, 则

  $
  y tilde.eq Coeq(pr_1,pr_2)
  $

  一个*内部等价关系*是单射 $r->x times x$, 使每个测试对象 $w$ 上的 $Hom_cal(E) (w,r)$ 都给出 $Hom_cal(E) (w,x)$ 上的等价关系. 两个投影记为 $r pairarrow x$. 这样的关系都有商 $q:x->Q$, 即这两个投影的余等化子, 并且是*有效的*:

  $
  r tilde.eq x times_Q x
  $

  上述满射及等价关系的商都对任意换基稳定. 因而 Grothendieck 意象是 Barr-正合范畴.
]

#proofsketch[
  在预层范畴中, 像与等价关系的商都逐点在 $Set$ 中计算. 对层态射 $f:x->y$, 记其预层像为 $P$, 层化得到 $Im(f):=a(P)$. 左正合性保证 $Im(f)->y$ 仍为单射, 而 $x->Im(f)$ 局部满射. 若 $f$ 本身是满射, 每个 $y$ 中的截面都局部来自 $P$, 因而 $a(P) tilde.eq y$. 这一步使用了层态射的局部满射判别, 不要求每个 $x(u)->y(u)$ 都满射.

  预层中, $x->P$ 是核偶的余等化子. 层化作为左伴随保持余等化子, 又因左正合而保持核偶, 所以层中的满射有效. 对内部等价关系 $r pairarrow x$, 同样先取逐点商再层化, 就得到以 $r$ 为核偶的商. 局部提升在拉回后仍成立, 所以满射对换基稳定; 核偶也与拉回相容, 因而这些商对换基稳定.
]

#proposition(title:[余积不交且对换基稳定])[
  Grothendieck 意象中的小余积彼此不交: 各余积注入都是单射, 不同分支的纤维积为初对象. 对任意 $x,y in cal(E)$, 有

  $
  x times_(x coprod y) y tilde.eq 0
  $

  并且对任意态射 $t->x coprod y$, 典范态射

  $
  (t times_(x coprod y) x)
  coprod
  (t times_(x coprod y) y)
  ->
  t
  $

  是同构. 因而 $cal(E)$ 是一个广延范畴; 更一般的小余积也具有同样的不交性与换基稳定性.
]

#proofsketch[
  在预层范畴中, 余积逐点是集合的不交并, 所以注入是单射, 不同分支的纤维积是初对象. 层化保持单射, 初对象与纤维积, 并将预层余积送到层余积, 因而余积在 $cal(E)$ 中仍不交.

  对任意 $t->x coprod y$, 已证的局部 Cartesian 闭性保证拉回函子有右伴随, 所以它保持余积. 将切片中的余积分解 $x coprod y$ 拉回到 $t$, 就得到命题中的同构. 任意小余积的论证相同.
]

#proposition(title:[所有余极限都是普遍的])[
  对任意态射 $f:t->s$, 换基函子

  $
  f^*:cal(E) slash s -> cal(E) slash t
  $

  保持所有小余极限. 等价地, 若 ${x_i}_(i in I)$ 是 $cal(E) slash s$ 中的图式, 则

  $
  f^* (colim_(i in I) x_i)
  tilde.eq
  colim_(i in I) f^* x_i
  $

  因而称 Grothendieck 意象中的余极限是*普遍的* (universal).
]

#proofsketch[
  局部 Cartesian 闭性说明 $f^*$ 存在右伴随 $Pi_f$. 因此 $f^*$ 本身是一个左伴随, 而左伴随保持所有余极限. 这里必须在切片范畴中理解余极限, 这正好表达了"先粘合再换基"与"先换基再粘合"给出相同结果.
]

#proposition(title:[滤过余极限与有限极限交换])[
  设 $I$ 是小滤过范畴, $K$ 是有限范畴, 而 ${x_(i k)}$ 是 $cal(E)$ 中以 $(i,k) in I times K$ 为指标的图式. 则自然态射给出同构

  $
  colim_(i in I) lim_(k in K) x_(i k)
  tilde.eq
  lim_(k in K) colim_(i in I) x_(i k)
  $

  也就是说, Grothendieck 意象中的滤过余极限是正合的.
]

#proofsketch[
  在 $Set$ 中, 滤过余极限与有限极限交换; 预层范畴逐点计算, 所以同一结论先在 $PSh(C)$ 中成立. 在 $cal(E)$ 中计算余极限需要层化, 但层化 $a$ 保持有限极限. 因而

  $
  lim_(k in K) a (colim_(i in I) x_(i k))
  tilde.eq
  a (lim_(k in K) colim_(i in I) x_(i k))
  tilde.eq
  a (colim_(i in I) lim_(k in K) x_(i k))
  $

  最右端正是 $cal(E)$ 中相应的滤过余极限.
]

=== 小生成族与局部可呈示性 <sec-topoi-generators>

#proposition(title:[层化可表对象生成整个意象])[
  层化可表对象

  $
  {a(h_u)}_(u in C)
  $

  构成 $cal(E)$ 的一小族生成元. 也就是说, 若 $f,g:x->y$ 满足对每个 $u in C$ 和每个态射 $h:a(h_u)->x$ 都有

  $
  f compose h=g compose h
  $

  则 $f=g$. 此外, 每个 Grothendieck 意象都是局部可呈示范畴.
]

#proofsketch[
  层化伴随与 Yoneda 引理给出

  $
  Hom_cal(E) (a(h_u),x)
  tilde.eq
  Hom_(PSh(C)) (h_u,i(x))
  tilde.eq
  x(u)
  $

  若两个层态射 $f,g:x->y$ 不相等, 它们必在某个 $u in C$ 的某个截面上取值不同. 这个截面对应一个态射 $a(h_u)->x$, 它便能区分 $f$ 与 $g$. 因为 $C$ 小, 这些生成元确实只组成一个集合.

  预层范畴 $PSh(C)$ 是局部可呈示的. 层条件可以表示为对所有覆盖筛包含 $cal(R)->h_u$ 的局部性条件; 当 $C$ 小时, 这些态射也只组成一个集合. 因而层化是一个可达左正合局部化, 其局部对象组成的 $Sh(C,J)$ 仍是局部可呈示范畴.
]

#theorem(title:[1-Giraud 定理])[
  设 $cal(E)$ 是一个局部小范畴. 在通常的集合大小约定下, 下列条件等价:

  1. $cal(E)$ 是 Grothendieck 意象.

  2. $cal(E)$ 满足以下 Giraud 公理:

     - $cal(E)$ 有有限极限与所有小余极限.

     - 小余积彼此不交.

     - 每个满射都是其核偶的余等化子.

     - 每个内部等价关系都是有效的.

     - 所有小余极限对任意换基稳定, 即都是普遍的.

     - $cal(E)$ 有一小族生成元.

  这里采用显式列出有效满射与普遍余极限的经典表述. 参见 @CarTT[第 21-23 页].
]

#proofsketch[
  从第一条到第二条正是本节前面各命题的内容: 在预层范畴中, 相应性质逐点归结为 $Set$; 左正合层化再把这些性质传递给层范畴. 层化可表对象 ${a(h_u)}_(u in C)$ 则给出一小族生成元.

  反过来, 假设 $cal(E)$ 满足 Giraud 公理. 先选择一小族生成元, 并将它扩充为一个对有限极限封闭的小满子范畴 $C subset.eq cal(E)$. 在 $C$ 上声明一族态射 ${u_i->u}$ 覆盖 $u$, 当且仅当诱导态射

  $
  coprod_i u_i -> u
  $

  是满射. 有效满射及其换基稳定性保证这些覆盖满足换基和传递公理, 因而定义一个 Grothendieck 拓扑 $J$.

  接着考虑限制 Yoneda 函子

  $
  N:cal(E) -> PSh(C),
  quad
  N(x) (u):=Hom_cal(E) (u,x)
  $

  有效满射的下降性质说明 $N(x)$ 对 $J$ 是层. 生成元保证 $N$ 忠实; 任意对象都能由生成元的余积满射覆盖, 再把这个满射的核偶取有效商, 可知自然变换能够唯一下降为 $cal(E)$ 中的态射, 所以 $N$ 还是全的.

  为了构造反方向, 对 $J$-层 $F$, 记 $"El"(F)$ 为其元素范畴: 对象是 $(u,s)$, 其中 $s in F(u)$; 态射 $(v,t)->(u,s)$ 是满足 $F(f)(s)=t$ 的 $f:v->u$. 在 $cal(E)$ 中定义

  $
  L(F):=colim_((u,s) in "El"(F)) u
  $

  余极限的泛性质给出 $L tack.l N$. 生成族与有效满射保证余单位 $L N(x)->x$ 是同构. 验证单位 $F->N L(F)$ 还需要用到余极限的换基稳定性与有效等价关系: 它们保证余极限中的截面与相等关系可以局部从表示图表中读出, 再由 $F$ 的层条件完成粘合. 这是逆向证明的关键下降论证, 细节参见 @CarTT[第 22-23 页]. 因而 $N$ 诱导等价

  $
  cal(E) tilde.eq Sh(C,J)
  $

  这便从纯粹内在的 Giraud 公理重新构造出了一个景表示.
]

== 无穷意象

#link(<sec-giraud>)[上一节]把普通意象写成了预层范畴经过层化得到的范畴, 而层化是一个保持有限极限的左伴随. 现在把局部信息从集合换成生象, 就可以沿着同样的路线引入无穷意象. 局部对象之间的相容关系也随之包含同伦, 以及这些同伦之间的更高相容性.

本节沿用#chapter-ref(<chap-animae>)的无穷范畴语言. 普通范畴出现时默认通过神经看成无穷范畴, 极限与余极限也在这个意义下理解. 我们仍固定一个集合论宇宙, 所讨论的范畴均局部小.

=== 生象值预层

回顾#chapter-ref(<chap-animae>), 对小无穷范畴 $C$, 其生象值预层范畴为
$ cal(P)(C):=Fun(C^opp,Ani) $
一个预层 $F$ 给每个 $u in C$ 指定生象 $F(u)$, 并沿态射给出限制映射及全部相容同伦. 这里保留 $PSh(C)$ 表示前文的集合值预层范畴, 用 $cal(P)(C)$ 表示生象值版本.

Yoneda 嵌入仍由 $y(u)(v)=Map_C (v,u)$ 给出. $cal(P)(C)$ 中的小极限与小余极限逐点计算. 接下来, 我们希望从这个足够大的范畴中选出满足某种粘合要求的对象.

=== 可达性与可呈示性

前面的普通意象有一小族生成元. 在无穷范畴中, 我们也需要控制一个大范畴怎样由较小的对象生成. 为此先说明"滤过"与"紧"的含义.

称无限基数 $kappa$ 为*正则基数*, 若任取少于 $kappa$ 个集合, 只要每个集合的基数都小于 $kappa$, 它们的并的基数也小于 $kappa$. 以下 $kappa$ 均取小的无限正则基数. 称一个单纯集为 *$kappa$-小的*, 若它的非退化单纯形总数小于 $kappa$.

#definition(title: [$kappa$-滤过与滤过余极限])[
  无穷范畴 $J$ 称为 *$kappa$-滤过的*, 若对每个 $kappa$-小单纯集 $K$, 每个图表 $K->J$ 都存在余锥. 也就是说, 可以给这张图表添加一个共同的目标对象, 以及指向它的相容态射和高阶同伦. 参见 @Ker26[Tag 05X0].

  以小的 $kappa$-滤过无穷范畴为指标的余极限, 称为 *$kappa$-滤过余极限*. 当 $kappa=aleph_0$ 时, 简称*滤过*与*滤过余极限*; 此时只需对有限单纯集检验余锥条件.
]

#definition(title: [$kappa$-紧对象])[
  设 $C$ 具有所有小 $kappa$-滤过余极限. 对象 $u in C$ 称为 *$kappa$-紧的*, 若 $Map_C (u,-)$ 保持这些余极限. 即对每个小 $kappa$-滤过图表 $F:J->C$, 典范映射
  $ colim_(j in J) Map_C (u,F(j)) -> Map_C (u,colim_(j in J) F(j)) $
  是生象的等价. 当 $kappa=aleph_0$ 时简称*紧对象*. 参见 @Ker26[第 9.2.5 节].
]

例如, 在 $Set$ 中, 紧对象恰好是有限集合, 而每个集合都是其有限子集的滤过余极限. 从有限集合到这个并的映射, 总会落在某个有限子集中; 上面的定义把这种性质连同映射之间的高阶信息一起保留.

#definition(title: "可达与可呈示无穷范畴")[
  无穷范畴 $C$ 称为 *$kappa$-可达的*, 若它具有所有小 $kappa$-滤过余极限, 且存在由 $kappa$-紧对象组成的小满子范畴 $C_0 subset.eq C$, 使每个 $C$ 中的对象都可表示为一个取值于 $C_0$ 的小 $kappa$-滤过图表的余极限.

  若这样的 $kappa$ 存在, 则称 $C$ *可达* (accessible). 若 $C$ 还具有所有小余极限, 则称它*可呈示* (presentable). 在普通范畴的语境中, 通常称为*局部可呈示*, 与#link(<sec-topoi-generators>)[小生成族一节]的用语一致. 参见 @Ker26[Tags 06K7, 06ND].
]

#definition(title: "可达函子")[
  对可达无穷范畴 $C,D$, 函子 $F:C->D$ 称为*可达的*, 若对某个小无限正则基数 $kappa$, $C,D$ 均具有小 $kappa$-滤过余极限, 且 $F$ 保持这些余极限. 参见 @Ker26[Tag 06KY].
] <def-accessible-functor>

对每个小 $C$, 生象值预层范畴 $cal(P)(C)$ 都是可呈示的. 可达性给出了生成对象所需的大小控制, 可呈示性则进一步保证任意小图表都可以取余极限.

=== 局部化与左正合

普通层化把一个预层送到与它对应的层, 并且对已经是层的对象不再作改变. 下面的伴随关系抽象了这种构造.

#definition(title: "反射局部化")[
  设 $i:E->D$ 是全忠实函子, 并存在左伴随 $L:D->E$. 我们称 $E$ 是 $D$ 的*反射子范畴*, 称 $L$ 为*反射局部化*, 本节简称*局部化*. 伴随的余单位给出 $L i tilde.eq id_E$.

  对任意 $u in D$, 单位 $eta_u:u->i L(u)$ 给出它在反射子范畴中的通用近似: 对每个 $v in E$, 有自然等价
  $ Map_E (L(u),v) tilde.eq Map_D (u,i(v)) $
  因而从 $u$ 映向反射子范畴中的对象, 等价于从 $L(u)$ 映向它. 参见 @Ker26[第 6.2.2 节].
]

#definition(title: "可达局部化与左正合局部化")[
  设 $D$ 可呈示, $L:D->E$ 是上述局部化, 右伴随为 $i$. 若自函子
  $ i L:D->D $
  可达, 则称这个局部化*可达*. 此时 $E$ 也是可呈示无穷范畴. 这里考察的是 $i L$; $L$ 作为左伴随本来就保持在目标范畴 $E$ 中计算的余极限. 参见 @Ker26[Tags 06U6, 06V8].

  若 $L$ 保持有限极限, 则称它*左正合* (left exact). 等价地, 它保持终对象与拉回, 即相应的典范映射给出
  $ L(1_D) tilde.eq 1_E, quad L(u times_w v) tilde.eq L(u) times_(L(w)) L(v) $
  这里两边的拉回都保留同伦数据. 左正合性保证局部化与取有限的相容条件能够交换. 参见 @Ker26[第 9.3.5 节].
]

#example(title: "局部化未必左正合")[
  #chapter-ref(<chap-animae>)的 $0$-截断 $tau_(<=0):Ani->Set$ 是局部化, 但不保持拉回. 圆周的例子给出
  $ tau_(<=0) (* times_(S^1) *) tilde.eq ZZ, quad * times_(tau_(<=0) S^1) * tilde.eq * $
  所以左正合性是一项额外要求: 仅仅能够把对象送进一个反射子范畴还不够.
]

=== 无穷意象的定义

#definition(title: "无穷意象")[
  无穷范畴 $cal(X)$ 称为*无穷意象*或 *$oo$-意象* ($oo$-topos), 若存在小无穷范畴 $C$ 及伴随
  $ a:cal(P)(C)->cal(X), quad i:cal(X)->cal(P)(C), quad a tack.l i $
  使 $i$ 全忠实, 且 $a$ 是可达的左正合局部化. 参见 @Lur09[定义 6.1.0.4].
] <def-infty-topos>

在这个定义中, $a$ 扮演广义层化的角色, $cal(X)$ 则组织层化后保留下来的对象及其高阶态射. 由可达局部化的性质, 每个无穷意象都是可呈示的.

#example(title: "生象与生象值预层")[
  恒等函子是可达左正合局部化, 所以 $cal(P)(C)$ 本身就是无穷意象. 特别地, 取 $C=*$, 得到
  $ cal(P)(*) tilde.eq Ani $
  因而生象范畴 $Ani$ 是最基本的无穷意象.
]

=== 取值于生象的层

现在把前面的层条件完整地提升到 $Ani$. 集合值层只需粘合截面与相等关系; 这里还要一起粘合路径, 路径之间的同伦, 以及全部更高相容性.

先允许景 $(C,J)$ 中的 $C$ 是小无穷范畴. 此时 $u$ 上的筛可以看作切片 $C slash u$ 中对预合成封闭的满子无穷范畴, 也等价于可表预层 $y(u)$ 的子对象
$ cal(R) arrow.r.hook y(u) $
这里的单态射指逐点的同伦纤维为空或可缩. 覆盖筛仍满足前文的最大筛, 拉回稳定与局部性公理, 拉回在无穷范畴中理解. 若 $C$ 是普通范畴, 就是把前文的 $cal(R) subset.eq h_u$ 看作离散生象值预层. 参见 @Lur09[定义 6.2.2.1, 命题 6.2.2.5].

#definition(title: [取值于 $Ani$ 的层])[
  一个*生象值预层*是无穷函子
  $ F:C^opp -> Ani $
  若对每个 $u in C$ 和每个覆盖筛 $cal(R) arrow.r.hook y(u)$, 限制映射
  $ F(u) tilde.eq Map_(cal(P)(C)) (y(u),F) -> Map_(cal(P)(C)) (cal(R),F) $
  都是*生象的等价*, 则称 $F$ 为 $(C,J)$ 上的*生象值层*或 *$Ani$ 值层* (sheaf of spaces). 参见 @Lur09[定义 6.2.2.6].

  这些层组成满子无穷范畴
  $ Sh_(Ani) (C,J) subset.eq cal(P)(C)=Fun(C^opp,Ani) $
  层之间的态射就是预层之间的自然变换, 连同自然变换之间的全部高阶同伦.
] <def-ani-sheaf>

右侧的 $Map(cal(R),F)$ 是覆盖上的*相容局部数据所组成的生象*. 若用 $C_cal(R) subset.eq C slash u$ 表示这个筛对应的满子范畴, 也可以把层条件写成
$ F(u) ->^~ lim_((v->u) in C_cal(R)^opp) F(v) $
极限在 $Ani$ 中计算. 因而"唯一粘合"的准确含义是: *给定一组完整的下降数据, 整体截面连同它与这些局部数据的识别, 组成一个可缩生象*. 这说的是上述限制映射的同伦纤维可缩, 并不要求 $F(u)$ 本身可缩.

#proposition(title: [覆盖族形式的 Čech 下降])[
  设 $C$ 是普通范畴, $J$ 由前文的预拓扑给出, 且所需纤维积存在. 预层 $F:C^opp->Ani$ 是层, 当且仅当对每个覆盖族 $cal(V)={u_i->u}_(i in I)$, 都有自然等价
  $ F(u) ->^~ lim_([n] in Delta) product_((i_0,dots,i_n) in I^(n+1)) F(u_(i_0) times_u dots times_u u_(i_n)) $
  这里 $Delta$ 是#chapter-ref(<chap-animae>)的单纯范畴. 右侧是一个余单纯生象的极限, 也称*全化* (totalization); 其中的映射由纤维积的投影与对角线诱导. 所有乘积和极限都在 $Ani$ 中计算.
] <prop-ani-cech-descent>

#proofsketch[
  在预层范畴中令 $v=coprod_i y(u_i)$, 并记这个覆盖族生成的筛为 $cal(R)$. 态射 $v->y(u)$ 的 Čech 神经逐点是集合映射的 Čech 神经, 其几何实现正是像 $cal(R)$: 在像中的每一点上, 分解方式组成非空集合, 相应的 Čech 神经可缩.

  对这个几何实现取到 $F$ 的映射生象, 就把余极限变成极限. 再由 Yoneda 引理, 得到命题右侧的公式. 因而它等价于 @def-ani-sheaf 中对生成覆盖筛的条件; 检验这些筛便足够.
]

这个极限同时记录所有层次: $n=0$ 给出各 $u_i$ 上的局部截面, $n=1$ 给出重叠处的路径, $n=2$ 说明路径的复合怎样相容, 再往上继续记录高阶同伦. 只写两个乘积之间的等化子, 一般会丢掉这些数据.

若 $F$ 取值于离散生象, 相容路径退化为相等关系, 就还原集合值层. 若取值于 $1$-截断生象, 则得到群胚值叠: 局部对象靠同构粘合, 同构满足余循环条件. #link(<sec-truncated-sheaves-stacks>)[下一节]会详细说明这两个特例.

#proposition(title: "生象值层化")[
  包含函子 $i:Sh_(Ani) (C,J)->cal(P)(C)$ 有左伴随
  $ a:cal(P)(C)->Sh_(Ani) (C,J) $
  且 $a$ 是可达左正合局部化. 因而 $Sh_(Ani) (C,J)$ 是无穷意象. 参见 @Lur09[命题 6.2.2.7].

  具体地, 层中的极限逐点计算; 余极限则先在预层中逐点计算, 再层化. 这与前文集合值层的计算规则一致.
]

#remark(title: "层条件与超下降")[
  上面的层条件要求覆盖筛下降, 在覆盖族的表述中就是完整的 Čech 下降. 若还要求对所有*超覆盖* (hypercover) 下降, 则称为*超完备层* (hypercomplete sheaf). 超覆盖允许在各级相容条件上继续选择覆盖; 这一般是更强的要求, 不能从"已经保留全部高阶同伦"直接推出. 本书的 $Sh_(Ani)$ 默认采用前面的层条件.

  有限截断的层自动超完备, 所以集合值层与群胚值叠不受这个区别影响. 参见 @Lur09[引理 6.5.2.9, 推论 6.5.3.13].
]

== 从截断回到层与叠 <sec-truncated-sheaves-stacks>

#chapter-ref(<chap-animae>)里, 生象的 $0$-截断还原集合, $1$-截断还原群胚. 现在加入局部性, 同样的两个层次就分别成为集合值层与群胚值叠. 我们先在无穷意象内部定义截断, 再在普通景上把这句话展开. 以下的"叠"均指*群胚值叠* (stack in groupoids).

=== 无穷意象中的截断

#definition(title: [无穷意象中的 $n$-截断对象])[
  设 $cal(X)$ 是无穷意象, $n>=0$ 为整数. 对象 $F in cal(X)$ 称为 *$n$-截断的*, 若对每个测试对象 $u in cal(X)$, 生象
  $ Map_cal(X) (u,F) $
  都是 $n$-截断的. 这些对象组成全子范畴 $cal(X)_(<=n) subset.eq cal(X)$. 特别地, $0$-截断对象也称为*离散对象*. 参见 @Lur09[定义 5.5.6.1].
]

这里的条件涉及所有测试对象. 它要求每一族以 $u$ 为参数的截面都只带有至多 $n$ 阶的同伦信息, 单独检查全局截面 $Map_cal(X) (1,F)$ 并不够.

#proposition(title: "截断函子")[
  包含函子 $cal(X)_(<=n)->cal(X)$ 具有左伴随
  $ tau_(<=n)^cal(X):cal(X)->cal(X)_(<=n) $
  称为 $cal(X)$ 中的 *$n$-截断函子*. 单位 $F->tau_(<=n)^cal(X) F$ 对任意 $E in cal(X)_(<=n)$ 诱导自然等价
  $ Map_cal(X) (tau_(<=n)^cal(X) F,E) tilde.eq Map_cal(X) (F,E) $
  因而截断是将 $F$ 映向一个 $n$-截断对象的通用方式. 参见 @Lur09[命题 5.5.6.18].
]

为看清这些对象的经典含义, 以下固定普通小景 $(C,J)$, 令
$ cal(X):=Sh_(Ani) (C,J) $
使用覆盖族时, 假设 $J$ 由前文的预拓扑给出, 且所需纤维积存在.

#proposition(title: "截断性质可以逐点检验")[
  对 $F in cal(X)$, 下列条件等价:
  - $F$ 是 $cal(X)$ 中的 $n$-截断对象.
  - 对每个 $u in C$, $F(u)$ 都是 $n$-截断生象.
]

#proofsketch[
  记层化为 $a:cal(P)(C)->cal(X)$. 由伴随与 Yoneda 引理,
  $ F(u) tilde.eq Map_cal(X) (a y(u),F) $
  所以第一条蕴含第二条. 反过来, 逐点 $n$-截断的预层就是 $cal(P)(C)$ 中的 $n$-截断对象; 利用层范畴的包含全忠实, 即得第一条.
]

=== 第零层的集合值层

#proposition(title: "集合值层就是离散对象")[
  将集合看作离散生象, 给出等价
  $ N(Sh(C,J)) tilde.eq cal(X)_(<=0) $
  这里 $N$ 表示普通范畴的神经. 因而 $cal(X)$ 的离散对象及其态射, 恰好还原前文的 Grothendieck 意象.
]

具体地, 若 $F$ 逐点离散, 覆盖上的相容路径就变成截面之间的相等. 生象值层的下降条件因此恰好成为等化子图
$ F(u) -> product_i F(u_i) pairarrow product_(i,j) F(u_i times_u u_j) $
也就是说, 在重叠处相等的一族局部截面, 唯一地来自一个整体截面. 这就是#chapter-ref(<sec-grothendieck-geometry>)开头连续函数的粘合规律.

更一般地, 对任意无穷意象 $cal(X)$, $cal(X)_(<=0)$ 都等价于某个普通 Grothendieck 意象的神经. 所以即使没有事先选定普通景, 也能在无穷意象内部认出经典的层范畴. 参见 @Lur09[定理 6.4.1.5, 注 6.4.1.3].

=== 第一层的群胚值叠

再保留一阶信息, 每个 $F(u)$ 就可以用普通群胚描述. 其中的点是 $u$ 上的对象, 路径是对象之间的同构. 在重叠处粘合时, 我们需要指定一个同构, 并说明这些同构怎样相容.

#definition(title: "群胚值预层与下降数据")[
  一个*群胚值预层* $cal(F)$ 给每个 $u in C$ 指定群胚 $cal(F)(u)$, 给每个 $f:v->u$ 指定限制函子 $f^*:cal(F)(u)->cal(F)(v)$. 限制与复合通过指定的自然同构相容, 这些同构满足单位与结合的相容条件. 这通常称为反变*伪函子*. 把群胚看成 $1$-截断生象后, 它就是取值于 $Ani_(<=1)$ 的预层.

  对覆盖族 $cal(V)={u_i->u}$, 记 $u_(i j)=u_i times_u u_j$, $u_(i j k)=u_i times_u u_j times_u u_k$. 一组*下降数据*由以下信息组成:
  - 局部对象 $x_i in cal(F)(u_i)$.
  - 重叠处的同构 $phi_(i j):x_j|_(u_(i j))->x_i|_(u_(i j))$, 满足三重重叠上的*余循环条件*
    $ phi_(i j) compose phi_(j k)=phi_(i k) $
    其中各同构都拉回到 $u_(i j k)$, 并使用限制函子的典范相容同构.

  从 $(x_i,phi_(i j))$ 到 $(x'_i,phi'_(i j))$ 的态射是一族同构 $psi_i:x_i->x'_i$, 在每个 $u_(i j)$ 上满足
  $ psi_i compose phi_(i j)=phi'_(i j) compose psi_j $
  这些数据组成*下降群胚* $"Desc"_cal(F) (cal(V))$. 参见 @Stacks26[Tag 02ZC].
] <def-groupoid-prestack>

#definition(title: "群胚值叠")[
  若群胚值预层 $cal(F)$ 对每个覆盖族 $cal(V)={u_i->u}$, 都使限制函子
  $ cal(F)(u)->"Desc"_cal(F) (cal(V)) $
  成为群胚的等价, 就称它为*叠*. 参见 @Stacks26[Tags 0268, 02ZH].
] <def-groupoid-stack>

这个等价包含两件事. *全忠实*说明同构可以唯一粘合: 对 $x,y in cal(F)(u)$, 预层
$ (v->u) |-> "Isom"_(cal(F)(v)) (x|_v,y|_v) $
是景 $C slash u$ 上的集合值层. *本质满*说明下降数据有效: 每组 $(x_i,phi_(i j))$ 都来自某个整体对象 $x in cal(F)(u)$ 及相容的局部同构. 连同这些局部同构一起, 粘合结果在唯一同构的意义下确定.

这也正是经典的纤维范畴表述. 将所有 $(u,x)$ 放在一起, 从 $(v,y)$ 到 $(u,x)$ 的态射取为 $(f,alpha)$, 其中 $f:v->u$, $alpha:y->f^* x$ 是指定的同构. 投影 $(u,x) |-> u$ 就给出一个纤维于群胚的范畴; 上述两项条件分别是同构层条件与对象的有效下降条件.

#proposition(title: [群胚值叠就是 $1$-截断对象])[
  对群胚值预层 $cal(F)$, 将各 $cal(F)(u)$ 取神经得到生象值预层 $N cal(F)$. 则
  $ cal(F) "是叠" quad <==> quad N cal(F) in cal(X)_(<=1) $
  每个 $cal(X)_(<=1)$ 中的对象都可如此得到. 对应关系还保留叠之间的态射与自然同构: $cal(X)_(<=1)$ 是经典叠的 $(2,1)$-范畴所对应的无穷范畴. 参见 @Hol08[定理 1.1, 第 3 节].
]

这里的原因可以直接从下降数据看出: 在 $1$-截断生象中, 重叠处的路径给出 $phi_(i j)$, 三重重叠处的相容同伦给出余循环等式. 更高相容性不再提供独立数据. 因而生象中的下降极限, 在群胚模型中正好由 $"Desc"_cal(F) (cal(V))$ 表示.

一个集合值层也可以看作只有恒等箭头的叠. 因此两个经典层次自然地嵌在一起:
$ N(Sh(C,J)) tilde.eq cal(X)_(<=0) subset.eq cal(X)_(<=1) subset.eq cal(X) $

=== 截断以后还需要层化

前面逐点检验的是"是否已经截断". 真正对一个生象值层作截断时, 还要保证截断后的局部信息能够粘合.

#proposition(title: "层中的截断公式")[
  设 $a:cal(P)(C)->cal(X)$ 是生象值层化, $i$ 是包含函子. 则对 $F in cal(X)$ 有自然等价
  $ tau_(<=n)^cal(X) F tilde.eq a(tau_(<=n)^(cal(P)(C)) i F) $
  右侧先对预层逐点作生象的 $n$-截断, 再层化. 这是左正合层化与截断相容的结果. 参见 @Lur09[命题 5.5.6.28].
]

当 $n=0$ 时, 记集合值层化为 $a_0$, 这个公式变成
$ tau_(<=0)^cal(X) F tilde.eq a_0(u |-> pi_0 (F(u))) $
若 $F$ 是叠, 右侧就是*对象的同构类预层所关联的层*. 它记录局部同构类, 会忘掉自同构, 也可能把整体不同而局部同构的对象视为相同.

当 $n=1$ 时, 则先逐点取基本群胚, 再作*叠化* (stackification). 更一般地, 对任意群胚值预层 $cal(F)$, $a(N cal(F))$ 仍然是 $1$-截断的, 对应的叠就是 $cal(F)$ 的叠化. 因而经典的层化与叠化, 分别是同一个生象值层化在第零层与第一层的表现.

#example(title: [从单对象群胚到分类叠 $B G$])[
  设 $G$ 是 $(C,J)$ 上的群层. 逐点取#chapter-ref(<chap-animae>)的单对象群胚, 得到预层 $u |-> N(bold(B)(G(u)))$. 定义其层化
  $ B G:=a(u |-> N(bold(B)(G(u)))) $
  它是一个 $1$-截断对象, 称为 $G$ 的*分类叠*. 这里 $B G$ 表示无穷意象中的对象; 在点上的情形, 它就回到#chapter-ref(<chap-animae>)的分类生象.

  具体地, $B G(u)$ 可用 $u$ 上的 $G|_u$-*挠子*及其等变同构组成的群胚描述. 挠子是带 $G|_u$ 右作用的层, 局部等变同构于带右乘作用的 $G|_u$ 自身. 原来的单对象群胚只给出平凡挠子; 叠化允许用重叠处的群元素作为粘合同构, 从而加入所有局部平凡的挠子. 参见 @Stacks26[Tags 04UJ, 04UV].

  每个挠子都局部平凡, 因此其同构类预层层化后只有一个截面:
  $ tau_(<=0)^cal(X) B G tilde.eq 1_cal(X) $
  但 $B G$ 本身仍保留平凡挠子的自同构群层 $G$. 例如, 在圆周的开集景上取常值群层 $ZZ slash 2 ZZ$, 平凡二重覆盖与连通二重覆盖给出两个不同的整体挠子, 却在足够小的开集上同构. 对应的挠子层由覆盖的局部截面组成. 所以逐点取同构类确实可能破坏层条件.
]
